# Copy this file to `.env` and replace the placeholder values with real keys.

# Anthropic — get a key from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-...

# DeepSeek — get a key from https://platform.deepseek.com/api_keys
# Only needed if you want to use the DeepSeek-side models in run_llm_test.py
# or draft_questions.py.
DEEPSEEK_API_KEY=sk-...
